Runnning the Application: 
To run the backend, run this command in the backend/ folder
fastapi dev app/main.py

To run the frontend, open another terminal and in the frontend/ folder run: 
npm run dev


Password for the Postgres is set to "postgres" temporarily


This is how to get into the virtual env that you should create. 
source .venv/bin/activate